# can we have 18(int) and 18(str) as two unique values in a set? Explain with an example.

set_example = {18, '18'}
print("The set contains:", set_example)
print("The length of the set is:", len(set_example))
