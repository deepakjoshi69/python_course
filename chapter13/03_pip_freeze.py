# pip freeze is a command to see which packages are installed globally or in isolated environment

# "pip freeze > requirements .txt" command to write all packages details in a txt file

# "pip install –r requirements.txt" using this command we can reinstall these requirements
